"""
Reaper: generated by earhart compile --target python.

Do not edit by hand. Source of truth is the .kinner.json.
"""
from __future__ import annotations

from kinner_runtime import Component, Transition


class Reaper(Component):
    """External cause-of-death sender. Pending -> Sent on send."""

    initial_state = 'Pending'
    state_constants = ('Pending', 'Sent',)
    _in_ports = ()
    _out_ports = ('KILL_OUT',)
    _observe_ports = ()
    _message_set = frozenset({'Kill'})

    def _build_transitions(self) -> list[Transition]:
        return _TRIPLES


_TRIPLES = [
    Transition(
        name='Reaper_ExternalDeathEventSIGTERMOOMTmuxPaneClose',
        from_state='Pending',
        to_state='Sent',
        kind='send',
        send_port='KILL_OUT',
        send_tag='Kill',
    ),
    Transition(
        name='Reaper_Terminal',
        from_state='Sent',
        to_state='Sent',
        kind='local',
    ),
]
