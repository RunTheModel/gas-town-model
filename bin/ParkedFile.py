"""
ParkedFile: generated by earhart compile --target python.

Do not edit by hand. Source of truth is the .kinner.json.
"""
from __future__ import annotations

from kinner_runtime import Component, Transition


class ParkedFile(Component):
    """Single-shot persistence record. NotPersisted -> Persisted on Write receive."""

    initial_state = 'NotPersisted'
    state_constants = ('NotPersisted', 'Persisted',)
    _in_ports = ('WRITE_IN',)
    _out_ports = ()
    _observe_ports = ()
    _message_set = frozenset({'Write'})

    def _build_transitions(self) -> list[Transition]:
        return _TRIPLES


_TRIPLES = [
    Transition(
        name='ParkedFile_PolecatParksGtParkWritesParkedAgentJson',
        from_state='NotPersisted',
        to_state='Persisted',
        kind='receive',
        recv_port='WRITE_IN',
        recv_tag='Write',
    ),
    Transition(
        name='ParkedFile_TerminalFilePersistsUntilCleanupOutOfScope',
        from_state='Persisted',
        to_state='Persisted',
        kind='local',
    ),
]
