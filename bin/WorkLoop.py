"""
WorkLoop: generated by earhart compile --target python.

Do not edit by hand. Source of truth is the .kinner.json.
"""
from __future__ import annotations

from kinner_runtime import Component, Transition


class WorkLoop(Component):
    """Polecat work-loop step that hits a gate. Pending -> Sent on observation that gate is Open."""

    initial_state = 'Pending'
    state_constants = ('Pending', 'Sent',)
    _in_ports = ()
    _out_ports = ('PARK_OUT',)
    _observe_ports = ('GATE_OBSERVE',)
    _message_set = frozenset({'Park'})

    def _build_transitions(self) -> list[Transition]:
        return _TRIPLES


_TRIPLES = [
    Transition(
        name='WorkLoop_WorkStepEncountersOpenGateEmitParkSignalToPolecat',
        from_state='Pending',
        to_state='Sent',
        kind='send',
        send_port='PARK_OUT',
        send_tag='Park',
        guard_fn=lambda self: (self._observed["GATE_OBSERVE"].state == 'Open'),
    ),
    Transition(
        name='WorkLoop_Terminal',
        from_state='Sent',
        to_state='Sent',
        kind='local',
    ),
]
