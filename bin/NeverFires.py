"""
NeverFires: generated by earhart compile --target python.

Do not edit by hand. Source of truth is the .kinner.json.
"""
from __future__ import annotations

from kinner_runtime import Component, Transition


class NeverFires(Component):
    """Single state, no transitions out. The OUT channel sits in NotSent forever."""

    initial_state = 'Quiet'
    state_constants = ('Quiet',)
    _in_ports = ()
    _out_ports = ('OUT',)
    _observe_ports = ()

    def _build_transitions(self) -> list[Transition]:
        return _TRIPLES


_TRIPLES = [
    Transition(
        name='NeverFires_Terminal',
        from_state='Quiet',
        to_state='Quiet',
        kind='local',
    ),
]
