"""
Worktree: generated by earhart --target python.

Do not edit by hand. Source of truth is the .kinner.json.
"""

from __future__ import annotations

from kinner_runtime import Component, Transition


class Worktree(Component):
    """On-disk worktree directory. Present after polecat spawn (initial state, since this component does not yet model the spawn step). Cleaned after Landing's CLEAN signal -- corresponds to removePolecatWorktree in cmd/convoy.go:1243-1255."""

    initial_state = 'Present'
    state_constants = ('Present', 'Cleaned',)
    _observe_ports = ()
    _in_ports = ('CLEAN',)
    _out_ports = ()

    def _build_transitions(self):
        return _TRIPLES


_TRIPLES = [
    Transition(
        name='Worktree_LandingFiresCLEANWorktreeDirectoryRemoved',
        from_state='Present',
        to_state='Cleaned',
        kind='receive',
        port='CLEAN',
        tag='Clean',
    ),
    Transition(
        name='Worktree_Terminal',
        from_state='Cleaned',
        to_state='Cleaned',
        kind='local',
    ),
]
