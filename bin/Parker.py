"""
Parker: generated by earhart compile --target python.

Do not edit by hand. Source of truth is the .kinner.json.
"""
from __future__ import annotations

from kinner_runtime import Component, Transition


class Parker(Component):
    """One-shot Park sender. Pending -> Sent on send."""

    initial_state = 'Pending'
    state_constants = ('Pending', 'Sent',)
    _in_ports = ()
    _out_ports = ('PARK_OUT',)
    _observe_ports = ()
    _message_set = frozenset({'Park'})

    def _build_transitions(self) -> list[Transition]:
        return _TRIPLES


_TRIPLES = [
    Transition(
        name='Parker_FiresParkModelingPolecatHitsAGateBlockingStep',
        from_state='Pending',
        to_state='Sent',
        kind='send',
        send_port='PARK_OUT',
        send_tag='Park',
    ),
    Transition(
        name='Parker_Terminal',
        from_state='Sent',
        to_state='Sent',
        kind='local',
    ),
]
