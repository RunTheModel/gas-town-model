"""
Bead: generated by earhart compile --target python.

Do not edit by hand. Source of truth is the .kinner.json.
"""
from __future__ import annotations

from kinner_runtime import Component, Transition


class Bead(Component):
    """Single bead's status as a typed integer (0=Open, 1=Hooked, 2=Closed) plus a formula identifier. Both initialized from per-instance options."""

    initial_state = 'Live'
    state_constants = ('Live',)
    _in_ports = ('HOOK',)
    _out_ports = ()
    _observe_ports = ()
    _option_defaults = {'initial': 0, 'formulaId': 1}
    _option_constants = {'Initial': 'initial', 'FormulaId': 'formulaId'}
    _typed_var_defaults = {'status': 'initial', 'formula': 'formulaId'}
    _message_set = frozenset({'Sling'})

    def _build_transitions(self) -> list[Transition]:
        return _TRIPLES


_TRIPLES = [
    Transition(
        name='Bead_SlingTargetsThisBeadStatus01OpenHooked',
        from_state='Live',
        to_state='Live',
        kind='receive',
        recv_port='HOOK',
        recv_tag='Sling',
        guard_fn=lambda self: ((self.status == 0)),
        assigns=(('status', lambda self: (self.status + 1)),),
    ),
]
