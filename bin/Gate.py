"""
Gate: generated by earhart compile --target python.

Do not edit by hand. Source of truth is the .kinner.json.
"""
from __future__ import annotations

from kinner_runtime import Component, Transition


class Gate(Component):
    """Single piece of state representing one gate. Open while waiting for the upstream dependency to resolve; transitions to Closed when Deacon's bd gate check confirms resolution."""

    initial_state = 'Open'
    state_constants = ('Open', 'Closed',)
    _in_ports = ('CLOSE_IN',)
    _out_ports = ()
    _observe_ports = ()
    _message_set = frozenset({'Close'})

    def _build_transitions(self) -> list[Transition]:
        return _TRIPLES


_TRIPLES = [
    Transition(
        name='Gate_DeaconSBdGateCheckClosesThisGateDependencyResolved',
        from_state='Open',
        to_state='Closed',
        kind='receive',
        recv_port='CLOSE_IN',
        recv_tag='Close',
    ),
    Transition(
        name='Gate_Terminal',
        from_state='Closed',
        to_state='Closed',
        kind='local',
    ),
]
