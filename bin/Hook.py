"""
Hook: generated by earhart --target python.

Do not edit by hand. Source of truth is the .kinner.json.
"""

from __future__ import annotations

from kinner_runtime import Component, Transition


class Hook(Component):
    """Lifecycle event firer. Real implementation: internal/hooks/config.go (type Hook). Each Hook instance fires once when its lifecycle moment is reached; the bound agent receives the event via the AGENT port and is responsible for handling. Generalization to multiple hook types and multi-fire (a single hook firing many times across an agent's lifetime) is deferred -- this is the simplest shape that captures the boundary."""

    initial_state = 'Idle'
    state_constants = ('Idle', 'Fired',)
    _observe_ports = ()
    _in_ports = ()
    _out_ports = ('AGENT',)

    def _build_transitions(self):
        return _TRIPLES


_TRIPLES = [
    Transition(
        name='Hook_LifecycleMomentReachedPreCompactFires',
        from_state='Idle',
        to_state='Fired',
        kind='send',
        port='AGENT',
        tag='PreCompact',
    ),
    Transition(
        name='Hook_TerminalHookFiresExactlyOncePerLifecycleMoment',
        from_state='Fired',
        to_state='Fired',
        kind='local',
    ),
]
